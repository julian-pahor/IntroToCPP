// ProblemTester.cpp : This file contains the 'main' function. Program execution begins and ends there.
//

#include <iostream>
#include "String.h"
#include "ItemProblem.h"
#include "Spyglass.h"
#include <vector>

int main()
{
    while (true)
    {
   /*     std::vector<std::unique_ptr<ItemProblem>> itemList;

        for (int i = 0; i < 3; i++)
        {
            std::unique_ptr<ItemProblem> ip = std::make_unique<Spyglass>();
            itemList.push_back(ip);
        }*/

        //std::vector<ItemProblem*> itemList;

        //for (int i = 0; i < 3; ++i)
        //{
        //    itemList.push_back(new Spyglass());
        //}

        //for (ItemProblem* ip : itemList)
        //{
        //    Spyglass* tester = dynamic_cast<Spyglass*>(ip);
        //    delete tester;
        //}

        //while (!itemList.empty())
        //{
        //    
        //    delete itemList.back();
        //    itemList.pop_back();
        //}
        //itemList.clear();
    }
}

// Run program: Ctrl + F5 or Debug > Start Without Debugging menu
// Debug program: F5 or Debug > Start Debugging menu

// Tips for Getting Started: 
//   1. Use the Solution Explorer window to add/manage files
//   2. Use the Team Explorer window to connect to source control
//   3. Use the Output window to see build output and other messages
//   4. Use the Error List window to view errors
//   5. Go to Project > Add New Item to create new code files, or Project > Add Existing Item to add existing code files to the project
//   6. In the future, to open this project again, go to File > Open > Project and select the .sln file
